package memo;

public class ArrayList_HashMap {
	/* 해시맵 키, 벨류 개념
	 * CRUD arry 로 가능 기능 제작 읽기 수정 삭제
	 * arrylist는 arr로는 불편한 기초적인 기능을 할 수 없기 때문에 사용
	 * 
	 * 리스트 선언문(초기화) : ArrayList or List<형 입력> ls= new ArrayList<형 입력>();
	 * 객체 추가 하기 : ls.add("Hi");      ls.add(1,"have:); (인덱스, 아이템)
	 * 특정 위치 객체 접근 : ls.get(1);
	 * 특정 위치 객체 삭제 : ls.remove(0);
	 * 리스트 삭제 : clear()
	 * 비어있는지 확인 isEmpty()
	 * 값 순회하기 이터레이터(순회자) iterator
	 * iterator<String> iter = ls.iterator();
	 * iter.hasNext() : 다음 인덱스가 비어있는지
	 * iter.next() : 이번 인덱스의 값을 가져온다
	 * 
	 */
}
